#include<bits/stdc++.h>

using namespace std;

int main() {
    int count = 0;
    int num;
    string stones;

    cin >> num >> stones;

    int length = stones.size();

    for (int i = 1; i < length; i++) {
        if (st)
    }
}